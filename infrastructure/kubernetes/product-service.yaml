apiVersion: apps/v1
kind: Deployment
metadata:
  name: product-deployment
  namespace: intelligent-payment
spec:
  replicas: 2
  selector:
    matchLabels:
      app: product-service
  template:
    metadata:
      labels:
        app: product-service
    spec:
      containers:
        - name: product-service
          image: intelligentpayment/product-service:1.0.0
          env:
            - name: SERVICE_NAME
              value: "product-service"
            - name: DATABASE_URL
              value: "postgresql+asyncpg://postgres:change_in_production_super_secret_db_password@postgres-service:5432/product_service_db"
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: platform-secrets
                  key: JWT_SECRET
          ports:
            - containerPort: 8002
          resources:
            requests:
              cpu: "150m"
              memory: "256Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
          readinessProbe:
            httpGet:
              path: /ready
              port: 8002
            initialDelaySeconds: 5
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /health
              port: 8002
            initialDelaySeconds: 10
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: product-service
  namespace: intelligent-payment
spec:
  selector:
    app: product-service
  ports:
    - port: 8002
      targetPort: 8002
